# Engineering AI Assistant (RAG Sample)

This is a minimal project scaffold demonstrating:
- FastAPI backend
- Svelte frontend
- RAG layer (embeddings + vector DB placeholder)
- LLM orchestration

Flow:
User -> FastAPI -> Retriever -> Vector DB -> LLM -> JSON Response
